<h1>🕊Twittery🕊</h1>

<div class="button-container">
    <%= link_to "新規投稿", new_post_path, class: "link-button" %> 
</div>

<ul>
<% @posts.each do |post| %>
    <li>
        <h2><%=post.title%></h2>
        <p><%=post.content%></p>
        <span>
            <small><%= post.created_at %></small>
        </span>
        <%
=begin%>
 <div class="edit-delete-wrapper">
            <%= link_to "編集", edit_post_path(post), class: "link-button" %>
            <%= button_to "削除", post_path(post), method: :delete, class: "link-delete-button", data: {confirm: "本当に削除しますか？"} %>
        </div> 
<%
=end%>
        <%= link_to "詳細", post_path(post), class: "link-button" %>
    </li>
    <% end %>
</ul>

